# Django Framework and Core Dependencies
Django>=5.2.0,<6.0.0
asgiref>=3.8.1
sqlparse>=0.5.0
tzdata>=2024.1; sys_platform == 'win32'

# Image Processing (Required for models.ImageField in adminapp)
Pillow>=10.0.0
